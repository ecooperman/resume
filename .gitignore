venv/
__pycache__/
*.pyc
.idea/

# The access-codes DB -- real data lives on the droplet, not in a local
# working copy. See ../DEPLOYMENT.md.
access.db
access.db.bak

# Generated by build.py -- evan-resume.yaml is the source of truth, these
# are build output and get regenerated on every deploy.
site/public/
site/private/

# Rach's own resume file - this repo (ecooperman/resume) is public on
# GitHub, so this must never be committed. Not a build artifact like the
# entries above; a deliberate privacy exclusion. See README.md.
rach-resume.yaml
